# 2026-07-13 Polar 프로덕션 온보딩 — 상품 생성·웹훅 등록·심사 제출 준비

> 샌드박스 검증 완료(7/12, 커밋 `4df713c`) 후 프로덕션 조직 온보딩을 이어서 진행 — 코드 변경 없음
> (커밋 0건), Polar 프로덕션 API 작업 + 환경파일 + 북키핑만. 작업 기간 2026-07-13~14.

## 배경

7/12 밤 세션에서 오너가 Polar 프로덕션 조직 "woong's company"를 생성하고 온보딩 2/8 단계까지
진행하다 중단됨(질문 미답변 상태로 세션 종료). 이번 라운드는 그 지점부터 재개: 컨텍스트 복구 →
프로덕션 토큰 수령 → 상품·웹훅 자동화 → 심사 제출 직전까지.

## 한 일

### 컨텍스트 복구 + 끊긴 질문 답변
7/12 세션이 답하지 못한 "대시보드 2개(dotori / woong's company) 정상이냐" 질문 해소 —
**dotori는 샌드박스**(sandbox.polar.sh, 로컬 `web/.env`가 사용 중), **woong's company는
프로덕션**(polar.sh)으로 별개 환경이 맞음. 샌드박스 토큰은 삭제하지 않고 유지(런치 전까지
로컬 개발·테스트용).

### 프로덕션 토큰 수령 + 상품 6개 생성 (코드 변경 없음)
- 오너 결정: 조직명은 "Dotori"로 바꾸지 않고 **"woong's company" 유지**.
- 토큰 검증: `GET /v1/organizations/`로 조직 확인(id `76342aed-19bf-4f08-8134-eb5768dcbe17`,
  slug `woongs-company`), 기존 상품 0개 확인 후 진행(중복 생성 방지).
- `tools/polar-setup.ts`(`POLAR_SERVER=production`)로 상품 6개 1회 성공:
  리딩 1/3/5장 = $4.99/$11.99/$17.99, 오라클 12/30/80개 = $1.99/$2.99/$5.99.
- 생성 후 `GET /v1/products/`로 6개 전부 재조회 — 이름·가격(센트)·usd·일회성(recurring
  None)·비보관(archived False) 모두 정확함을 실제 응답으로 확인.

### 프로덕션 웹훅 엔드포인트 등록
- 당초 "런치 시점에 처리" 계획이었으나, 심사 체크리스트의 Checkout integration 항목이 웹훅을
  요구해 앞당겨 등록: `POST /v1/webhooks/endpoints` → id `48efcd35-c827-4822-85aa-cce04e736d6e`,
  url `https://dotori-web.vercel.app/api/payments/polar/webhook`, 이벤트 `order.paid` /
  `checkout.expired` / `order.refunded` 3종(샌드박스와 동일 구성).
- 도메인 확정 시 엔드포인트 url만 PATCH하면 됨 — 시크릿 불변.

### 자격증명 보관 — `web/.env.production.local` (신규, gitignored)
프로덕션 토큰 + 상품 ID 6개 + 웹훅 시크릿(`whsec_...`)을 한 파일에 모음. `git check-ignore`로
`.env*.local` 규칙에 걸리는 것 확인(커밋 불가). 로컬 개발용 `web/.env`(샌드박스)는 무변경.
런치 시 이 파일 내용을 Vercel `dotori-web` 프로덕션 env로 복사하면 됨.

### 오너 대시보드 온보딩 가이드 (심사 제출 직전까지)
- Finance → Account에서 정산 계좌 연결 안내 — SWIFT/BIC 코드 설명(주요 시중은행 코드표 제공),
  계좌번호 하이픈 제거, 영문 예금주명 일치 주의. → **Payout account ✅ 완료됨**.
- Identity verification(신분증)·Social links(운세 콘텐츠 심사 대비 실존 운영자 증빙) 안내 →
  둘 다 ✅ 완료됨.
- Submit for review 버튼 비활성 이슈: 남은 시계 아이콘 항목 **Product website**가 원인으로 판단.
  `dotori-web.vercel.app`을 curl로 확인하니 **200 공개 서빙 중** — 7/12 세션의 "로그인 보호
  상태라 비워두라" 전제가 더 이상 사실이 아님을 확인하고, 해당 URL을 채우도록 안내 방침 변경.
  (부수 확인: 공개 상태이므로 웹훅 POST도 차단 없이 도달 가능.)

### 북키핑 정정·갱신 (이번 라운드의 유일한 레포 내 변경, 미커밋)
- `NEEDS_FROM_OWNER.md`: 샌드박스 항목 ✅ 정리(낡은 🔴 정정), 프로덕션 토큰 ✅, 웹훅 시크릿 ✅,
  **정산 계좌 연결=심사 시작을 새 🔴로 승격** 후 계좌 연결 완료까지 반영.
- `work-log/2026-07-12 ... Polar.sh로 결제 전환.md`: 커밋 `4df713c`가 이미 완료한 실결제 E2E가
  "남은 항목"에 🔴로 남아 있던 **문서-코드 불일치 정정**(커밋이 진실).

## 검증

- 상품: 생성 API 응답 + 별도 GET 재조회로 6/6 이름·가격·통화·타입 정확 (total_count 6).
- 웹훅: 생성 응답에서 id·url·이벤트 3종·`whsec_` 시크릿(49자) 확인.
- 프로덕션 URL: `curl -L` 200, Dotori 앱 HTML 서빙 확인.
- `web/.env.production.local`: `git check-ignore` 통과(web/.gitignore:8 `.env*.local`).
- 코드 변경이 없어 typecheck/test/build는 재실행하지 않음 (7/12 시점 208 passed 그린이 최종).

## 주요 의사결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 프로덕션 조직명 | "woong's company" 유지 (Dotori로 변경 안 함) | 오너 결정 |
| 웹훅 등록 시점 | 런치 시점 → 지금으로 앞당김 | 심사 체크리스트(Checkout integration)가 요구, URL은 도메인 확정 후 PATCH 가능 |
| Product website | 비워두기 → `dotori-web.vercel.app` 입력으로 변경 | 배포가 공개 상태(200)임을 실측 — 로그인 보호 전제 소멸, Submit 버튼 활성화 조건 + 심사팀에 실물 증명 |
| 프로덕션 자격증명 보관 | `web/.env.production.local` 신규 파일 | 로컬 `.env`(샌드박스) 오염 방지, gitignore 실측 확인, 런치 시 Vercel로 복사만 하면 되는 단일 출처 |

## 보류/이월

- [ ] **Submit for review 클릭 확인** — Product website 입력 후 버튼 활성화·제출 여부 미확인
  (오너 진행 중). 제출되면 심사 1~2주.
- [ ] 🔴 **마이그레이션 0002(`rate_limits`) 미실행** — 레이트리밋이 fail-open으로 전부 통과 중.
  Supabase SQL Editor에서 오너 실행 필요 (`NEEDS_FROM_OWNER.md`).
- [ ] 도메인 결정, 법적 정보 7개 값(`business-info.ts`), Vercel 프로덕션 env 설정(=
  `.env.production.local` 복사) — 심사와 병행 가능.
- [ ] 심사 통과 후: 프로덕션 실결제 스모크 테스트 1회(소액 결제→크레딧→환불).

## 다음 작업

1. 오너: Product website에 `https://dotori-web.vercel.app` 입력 → **Submit for review** 클릭,
   결과 공유 (막히면 캡쳐).
2. 심사 대기 중 병행: 0002 마이그레이션 실행 + 도메인·법적 정보 결정.
