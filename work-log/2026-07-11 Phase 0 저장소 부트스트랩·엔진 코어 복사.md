# Phase 0 — K-saju 저장소 부트스트랩 · 엔진 코어 복사

> 날짜: 2026-07-11
> 플랜: `~/.claude/plans/glimmering-roaming-moon.md` (Dotori — 영어권 K-saju 앱)

## 한 일

- `piLab/project/K-saju` 신규 git 저장소 생성 (한국판 saju의 형제 저장소, 한국판은 무변경)
- 루트 구성: `package.json`(dotori-engine) · `pnpm-workspace.yaml`(`.`,`web`) · tsconfig · vitest · `.gitignore`
- 한국판 엔진에서 언어 중립 코어 복사:
  - **verbatim**: `saju-engine/` 전체, `compatibility/`(teaser 제외), `llm/`, `reading/{generate,sanitize,types,chart-summary,character,summary}`, `menus/{_generate-section,paywall,types,solo,couple,love-marriage,career,teaser}`, `oracle/index`
  - **위치 이동**: `naming-engine/manseryeok-adapter` → `chart-input/`
  - **Phase 2용 스텁**: `reading/prompts/{frame-en-v1,modules-en,teaser-en}`, `menus/prompts-en`, `oracle/{prompt-en,questions-en}`, 영어 `guard.ts`, `i18n/glossary-en` 골격
  - **제외**: 작명 제품 파일 전부, `menus/{naming,newyear,reunion,intimacy}`, 한국어 프롬프트 원본, tools/
- `ENGINE_SYNC.md` 작성: 원본 SHA, 파일별 상태(verbatim/relocated/stub/dropped), 임포트 수정 라인 전수 기록, "verbatim 파일은 Dotori에서 수정 금지" 규칙

## 명세 이탈 (판단 사항, ENGINE_SYNC.md에 상세)

1. `naming-engine/{llm,types,_element-tables}.ts` 원경로 보존 — saju-engine verbatim 파일들이 임포트하므로 필수. 한국어/작명 콘텐츠 없음
2. `menus/teaser.ts` 추가 복사 — 러너 4종이 하드 임포트
3. `compatibility/index.ts`에서 제외된 teaser export 1줄 제거 (영어 티저는 후속 페이즈에 `-en` 스텁 필요)
4. `menus/types.ts`에서 죽은 코드 `HanjaRepo`/naming deps 필드 제거 (MENU_IDS 8종은 테스트 호환 위해 유지)
5. 한국어 콘텐츠를 단언하는 테스트 3파일 드롭 + 2파일에서 서브테스트 2개 제거 → **Phase 2에서 영어판 등가 테스트 재작성 필요**

## 검증

- `pnpm typecheck` 통과, `pnpm test` 23파일/120테스트 그린 (검증자 직접 재실행)
- 한국판 저장소 `git status` clean 확인

## 다음

- Phase 1: `chart-input/international-birth.ts`(IANA tz → UTC → KST+9, adjustForDst 우회, 음수 경도 프로브), `i18n/glossary-en` 전체 채우기 + 완전성 테스트, `chart-summary-en`, sanitize `hasCjkLeak`, guard 영어 규칙 완성, 골든 테스트(NY/런던/베를린/LA/서울1988)
